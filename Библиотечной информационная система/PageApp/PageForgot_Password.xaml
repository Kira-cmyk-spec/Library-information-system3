<Page x:Class="Библиотечной_информационная_система.PageApp.PageForgot_Password"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Библиотечной_информационная_система.PageApp"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="PageForgot_Password">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="6*"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="5*"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="1" Grid.Column="1">
            <Label Content="Забыли пароль?" HorizontalAlignment="Center" Margin="20"/>
            <Label Content="Введите ваш Email или Логин и мы вышлем вам инструкции по получению нового пароля" Margin="10"/>
            <TextBox x:Name="email" Margin="20" Height="40"/>
            <Button x:Name="sms" Content="Подтвердить" Click="sms_Click"  Width="100"/>
        </StackPanel>
    </Grid>
</Page>
